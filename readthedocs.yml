version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.9"

python:
  install:
    - requirements: requirements.txt

mkdocs:
  configuration: mkdocs.yaml

# Run the script to generate index.md before building
build:
  jobs:
    pre_build:
      - python scripts/generate_toc.py
